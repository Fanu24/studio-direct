# Preserve the supplied specification byte-for-byte, including Markdown hard breaks.
docs/specs/nodework-product-update-2026-09.md -text -whitespace
# Reference manifests must have the same hashes on Windows and Linux.
packages/db/reference/*.json text eol=lf
packages/db/reference/ISO-LANGUAGES-LICENSE text eol=lf
packages/db/reference/GEONAMES-README.txt -text -whitespace
# Keep upstream font license notices intact.
apps/web/app/fonts/*-OFL.txt -text -whitespace
