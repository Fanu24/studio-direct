'use client';
import {useId,useState,type ReactNode} from 'react';
export function InfoTooltip({label,children}:{label:string;children:ReactNode}){const id=useId(),[open,setOpen]=useState(false);return <span style={{position:'relative',display:'inline-block'}} onMouseEnter={()=>setOpen(true)} onMouseLeave={()=>setOpen(false)} onFocus={()=>setOpen(true)} onBlur={e=>{if(!e.currentTarget.contains(e.relatedTarget))setOpen(false);}} onKeyDown={e=>{if(e.key==='Escape')setOpen(false);}}><button type="button" aria-label={label} aria-describedby={open?id:undefined} aria-expanded={open} onClick={()=>setOpen(v=>!v)}>ⓘ</button>{open?<span id={id} role="tooltip" style={{display:'block',position:'absolute',zIndex:20,insetInlineStart:0,width:'min(320px,75vw)',padding:12,background:'var(--surface, white)',color:'var(--text, black)',border:'1px solid currentColor'}}>{children}</span>:null}</span>;}
