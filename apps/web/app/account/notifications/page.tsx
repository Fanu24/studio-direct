import {redirect} from 'next/navigation';
import {platform,currentUser} from '../../../lib/platform';
export const dynamic='force-dynamic';
export const metadata={title:'Notification preferences',robots:{index:false,follow:false}};
export default async function Page(){const env=await platform(),user=await currentUser(env);if(!user)redirect('/login?next=/account/notifications');const rows=await env.DB.prepare('SELECT kind,email_enabled,in_app_enabled FROM notification_preferences WHERE user_id=?').bind(user.id).all<{kind:string;email_enabled:number;in_app_enabled:number}>();const kinds=['application_received','application_confirmation','application_stage','job_invitation','early_access_open','featured_member','review_published','review_response','job_expiry','low_credits','plan_renewal'];return <main className="container stack"><h1>Notification preferences</h1><p>Security and billing confirmations remain available.</p><form method="post" action="/api/product/notification-preferences">{kinds.map(kind=>{const row=rows.results.find(r=>r.kind===kind);return <fieldset key={kind}><legend>{kind.replaceAll('_',' ')}</legend><label><input name={kind+':email'} type="checkbox" value="1" defaultChecked={row?!!row.email_enabled:true}/>Email</label><label><input name={kind+':app'} type="checkbox" value="1" defaultChecked={row?!!row.in_app_enabled:true}/>In-app</label></fieldset>;})}<button>Save preferences</button></form><h2>Weekly jobs newsletter</h2><form action="/api/product/newsletter" method="post"><label><input name="consent" value="1" type="checkbox"/>Send me the weekly jobs newsletter</label><button name="action" value="subscribe">Subscribe</button><button name="action" value="unsubscribe">Unsubscribe</button></form></main>;}
